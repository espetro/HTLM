# runtime-bench

Minimal Chrome+Firefox WASM smoke-test harness. Measures per-step latency/memory of the exported model in both real browsers, not just Node.

Automation protocol pick (CDP vs WebDriver BiDi vs per-browser tools) deferred to when this harness is built — see `docs/feasibility.md`.
